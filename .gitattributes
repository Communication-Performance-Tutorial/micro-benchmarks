# Default: normalize to LF in the repo, convert on checkout per OS
* text=auto

# Force LF for scripts that must stay LF (e.g. run on Linux)
*.sh text eol=lf

# Force CRLF for Windows-only files
*.bat text eol=crlf